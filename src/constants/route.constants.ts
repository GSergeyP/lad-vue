export enum Links {
  MAIN = '/',
  SLIDER = '/slider',
  CARD = '/card',
  FORM = '/form',
  LIST = '/list',
  MODAL = '/modal',
  NOTFOUND = '/:pathMatch(.*)*'
}

export enum PathNames {
  MAIN = 'main',
  SLIDER = 'slider',
  CARD = 'card',
  FORM = 'form',
  LIST = 'list',
  MODAL = 'modal',
  NOTFOUND = 'not-found'
}
